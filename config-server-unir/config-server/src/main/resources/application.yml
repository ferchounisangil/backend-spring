server:
  port: 8888

spring:
  application:
    name: config-server

  profiles:
    active: native

  cloud:
    config:
      server:
        native:
          search-locations: file:///D:/Descargas/Esp software/Plataforma de desarrollo/microservicios/
      fail-fast: true

management:
  endpoints:
    web:
      exposure:
        include: '*'

spring.security.user.name: admin
spring.security.user.password: admin
